#!/bin/bash

set -o nounset
set -e 

### Input parameters ###
PERIOD=${1:-test}

CNT=3
case "$PERIOD" in
  daily   ) CNT=6 ;;
  weekly  ) CNT=4 ;;
  monthly ) CNT=3 ;;
  test    ) CNT=1 ;;
esac

echo $CNT
